node_modules
dist
dist-ssr
*.local
.DS_Store
*.log

# Service-account keys / credentials — never commit
*-key.json

# E2E harness artifacts (downloaded output, Playwright report/trace)
test/e2e/artifacts
.playwright/
test-results/

# Throwaway scratch (reports, spikes) — issues/PRDs under .scratch/ are tracked
.scratch/tmp/
